# https://kubernetes.io/docs/concepts/configuration/configmap/
kind: ConfigMap
apiVersion: v1
metadata:
  name: app-config
  namespace: default
data:
  app.how: very
  app.type: charm
  app.redis: "redis"

---
